class Song {

    constructor(url, title, thumbnail, duration) {
        this.url = url
        this.title = title
        this.thumbnail = thumbnail
        this.duration = duration
    }
}

module.exports = Song